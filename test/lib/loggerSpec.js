var should = require('should');

describe('Logger', function(){
    describe('When logging a message', function(){
        it('it should log the message to the console', function(){
            true.should.equal(true);
        })
    })
})